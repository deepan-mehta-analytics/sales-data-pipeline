# =============================================================================
# requirements-api.txt
# Runtime dependencies for the FastAPI query layer (v1.2).
#
# Isolated from requirements.txt so the pipeline Docker image stays lean —
# only the api-runtime Docker stage and the API CI step install these.
#
# Install with:  pip install -r requirements-api.txt
# =============================================================================

fastapi>=0.111.0          # Web framework — routes, dependency injection, OpenAPI docs
uvicorn[standard]>=0.29.0 # ASGI server for serving the FastAPI application
httpx>=0.27.0             # HTTP client required by FastAPI's TestClient for tests
